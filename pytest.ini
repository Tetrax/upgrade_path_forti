[pytest]
# Only the Playwright E2E suite -- tests/ (without e2e/) is the existing stdlib unittest suite,
# run via `python3 -m unittest discover -s tests`, not pytest.
testpaths = tests/e2e
addopts = --screenshot=only-on-failure --video=retain-on-failure --tracing=retain-on-failure
